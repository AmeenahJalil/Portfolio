import React from "react";
import '../App.css'

function Header() {
  return (
    <header>
        <nav className = "flex-container">
        
      
        <a href="#">Home</a>

        <a href="#">About</a>

        <a href="#">Projects</a>

        <a href="#">Contact</a>

        <a href="#">CV</a>
        </nav>
      
    </header>
  );
}
export default Header;
